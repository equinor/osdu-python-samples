extends: ["@commitlint/config-conventional"]
rules:
  type-enum:
    - 2
    - always
    - - feat
      - fix
      - deps
      - revert
      - docs
      - style
      - chore
      - refactor
      - ci
